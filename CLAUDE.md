# joucomi

## セッションのやり残しを残す運用（重要）

このリポジトリは、Web版セッションが中断されても「やり残し」を後から掘り起こせるように、
2つの仕組みで作業状態を GitHub に自動退避する。

### 1. 作業ジャーナル（あなた＝Claudeが書く）
意味のある作業（コード変更・調査・設計判断など）をしたターンの最後に、
`./.claude/journal/<YYYY-MM-DD>.md` に**短く**追記すること。フォーマット:

```
## <時刻 UTC> [session <id8>] ブランチ: <branch>
- やってること: <一言>
- 完了: <あれば>
- 未完(TODO): <次にやるべきこと。ここが一番大事>
- 中断したら次の一手: <コピペで再開できる指示>
```

純粋な雑談・質問応答だけのターンでは書かなくてよい。簡潔第一。長文にしない。

### 2. 自動チェックポイント（Stopフックが自動でやる）
`.claude/hooks/checkpoint.sh` が毎ターン終了時に発火し、作業ツリー（コード＋上記ジャーナル）を
セッション専用ブランチ `claude/wip/<date>-<session8>` にスナップショットして push する。
作業ブランチ・working tree は汚さない（`commit-tree` で別refに退避するだけ）。

→ つまり「ジャーナルを書く」だけでよい。GitHubへの退避はフックが自動でやる。

### 掘り起こし
`/recap` コマンドが上記の WIP ブランチとジャーナルを読み、中断・放置されたやり残しを報告する。
